package org.neil.fluff.sarcasticjava;

/** I don't know what this class is, but it's not a folder. */
public class FolderTest {
    @Test public void useTheSarcasticTypes() throws Exception {
        // Create a Folder
        ʺFolderʺ f = new ʺFolderʺ();

        // save the folder
        f.ʺsaveʺ();
    }
}
